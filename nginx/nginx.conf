server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location ~ ^/(register|login|token|todos) {
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}